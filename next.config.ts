import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    'weak-pugs-look.loca.lt',
    'shaggy-jobs-learn.loca.lt',
    'puny-lines-study.loca.lt',
    'soft-results-guess.loca.lt',
    'killing-when-later-charts.trycloudflare.com',
    'exclude-missing-acting-pads.trycloudflare.com',
  ],
};

export default nextConfig;
